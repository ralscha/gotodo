{
  "name": "gotodo-emails",
  "version": "1.0.0",
  "scripts": {
    "build": "mjml src/* -o ./output --config.minify true",
    "postbuild": "cd ../server && go run gotodo.rasc.ch/cmd/mailgen ../emails/output ./mails",
    "start": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mjml": "5.4.0"
  }
}
